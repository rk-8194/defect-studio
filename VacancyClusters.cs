using System;

namespace DefectStudio.Library
{
    public static partial class Command
    {
        public static void GenerateVacancyCluster(string[] args)
        {
            // Get the number of vacancies to place in the cell.
            int _number = int.Parse(args[0]);
        }
    }
}
